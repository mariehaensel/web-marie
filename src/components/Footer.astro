---
import { getCollection } from 'astro:content';
const [kontakt] = await getCollection('kontakt');
const year = new Date().getFullYear();
---
<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-contact-row">
      <div>
        <p class="footer-kicker">Kontakt</p>
        {kontakt?.data.email && <a class="footer-email" href={`mailto:${kontakt.data.email}`}>{kontakt.data.email}</a>}
      </div>
      {(kontakt?.data.instagram || kontakt?.data.whatsapp) && (
        <div>
          <p class="footer-kicker">Socials</p>
          <div class="footer-social-icons">
            {kontakt?.data.instagram && (
              <a class="footer-social" href={kontakt.data.instagram} target="_blank" rel="noopener" aria-label="Instagram">
                <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.7">
                  <rect x="3" y="3" width="18" height="18" rx="5.5"></rect>
                  <circle cx="12" cy="12" r="4.2"></circle>
                  <circle cx="17.4" cy="6.6" r="1.1" fill="currentColor" stroke="none"></circle>
                </svg>
              </a>
            )}
            {kontakt?.data.whatsapp && (
              <a class="footer-social" href={`https://wa.me/${kontakt.data.whatsapp}`} target="_blank" rel="noopener" aria-label="WhatsApp">
                <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.7">
                  <path d="M4 20l1.4-4.2A8 8 0 1 1 8.6 19L4 20z"></path>
                  <path d="M8.5 10.2c.4 2.6 2.4 4.6 5 5" stroke-linecap="round"></path>
                </svg>
              </a>
            )}
          </div>
        </div>
      )}
    </div>
    <div class="footer-meta">
      <p>&copy; {year} Marie Hänsel</p>
      <ul>
        <li><a href="/impressum">Impressum</a></li>
        <li><a href="/datenschutz">Datenschutz</a></li>
      </ul>
    </div>
  </div>
</footer>
